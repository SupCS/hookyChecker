[build]
builder = "DOCKERFILE"

[deploy]
preDeployCommand = ["hooky-checker init-db"]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
